
# 📘 Tuition Payment Management System

### (سیستم مدیریت شهریه دانشجویان)

## 📝 معرفی پروژه

این برنامه با زبان **C++** نوشته شده و هدف آن مدیریت اطلاعات شهریه و وضعیت پرداخت دانشجویان یک دانشگاه یا مؤسسه‌ی آموزشی است.
کاربر می‌تواند اطلاعات دانشجویان را ثبت کند، پرداخت‌های جدید انجام دهد، وضعیت پرداخت را بررسی کند و لیست کامل دانشجویان را مشاهده نماید.
تمام اطلاعات به‌صورت پایدار در یک فایل متنی ذخیره می‌شود تا پس از بستن برنامه نیز از بین نرود.

---

## 🎯 قابلیت‌های اصلی برنامه

### 1️⃣ افزودن دانشجوی جدید

* دریافت اطلاعات زیر از کاربر:

  * شماره دانشجویی
  * نام دانشجو
  * مبلغ کل شهریه
  * مبلغ پرداخت‌شده اولیه
* بررسی تکراری نبودن شماره دانشجویی
* ذخیره اطلاعات در فایل `tuition.txt`

---

### 2️⃣ ثبت پرداخت (پرداخت اقساطی)

* امکان جستجوی دانشجو **با شماره دانشجویی یا نام**
* افزودن مبلغ جدید به پرداخت‌های قبلی
* به‌روزرسانی اطلاعات دانشجو در فایل
* پشتیبانی از پرداخت چندمرحله‌ای (اقساطی)

---

### 3️⃣ جستجوی دانشجو

* جستجو بر اساس:

  * شماره دانشجویی
  * یا نام دانشجو
* نمایش اطلاعات کامل دانشجو:

  * شهریه کل
  * مبلغ پرداخت‌شده
  * وضعیت پرداخت (تسویه شده / بدهکار)

---

### 4️⃣ نمایش همه دانشجویان

* نمایش لیست کامل دانشجویان ثبت‌شده
* نمایش وضعیت پرداخت هر دانشجو
* خروجی خوانا و مرتب

---

### 5️⃣ مدیریت خطای ورودی

* جلوگیری از کرش برنامه در صورت وارد کردن داده‌ی نامعتبر
* بررسی ورودی‌های عددی با `cin.fail()`
* پیام‌های مناسب برای خطاهای کاربر

---

## 📁 ساختار فایل ذخیره‌سازی

اطلاعات دانشجویان در فایل متنی زیر ذخیره می‌شود:

```
tuition.txt
```

فرمت هر خط فایل:

```
StudentID  Name  TotalTuition  AmountPaid
```

مثال:

```
987654321 Ali 5000000 3000000
987654322 Sara 4500000 4500000
```

هر خط نماینده‌ی یک دانشجو است.

---

## 🧠 منطق تعیین وضعیت پرداخت

وضعیت پرداخت دانشجو به‌صورت **پویا** محاسبه می‌شود:

* اگر `مبلغ پرداخت‌شده < شهریه کل` → **بدهکار**
* اگر `مبلغ پرداخت‌شده ≥ شهریه کل` → **تسویه شده**

---

## 🧩 ساختار برنامه (فنی)

* استفاده از `struct daneshjo` برای نگهداری اطلاعات دانشجو
* استفاده از آرایه با ظرفیت 100 دانشجو برای پردازش داده‌ها
* استفاده از:

  * `ifstream` برای خواندن فایل
  * `ofstream` برای نوشتن در فایل
* جداسازی منطق برنامه در توابع مستقل از `main`
* استفاده از منوی تعاملی با `do-while` و `switch`

---

## ▶️ نحوه اجرای برنامه

1. برنامه را اجرا کنید
2. از منوی اصلی یکی از گزینه‌ها را انتخاب کنید
3. اطلاعات موردنظر را وارد نمایید
4. برای خروج، گزینه‌ی **5** را انتخاب کنید

---
